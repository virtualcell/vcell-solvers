see commented-out provisioning script at bottom of Vagrantfile ... requires manual provisioning

vagrant up
vagrant ssh
... follow instructions from bottom of Vagrantfile

vagrant ssh -c /vagrant/build.sh
